{
  "name": "power-panel",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "web": "bun --cwd apps/web run dev",
    "worker": "bun --cwd apps/worker run dev",
    "install:all": "bun install"
  },
  "dependencies": {
    "@types/bun": "^1.3.8"
  }
}
